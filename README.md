# UploadBot 🎬

Telegram-бот для автоматического создания Shorts/Reels/TikTok из YouTube видео.  
Скачивает видео, нарезает по сценам, конвертирует в вертикальный формат 9:16, добавляет субтитры и хештеги.

## Возможности

| Функция | Описание |
|---|---|
| 📥 Загрузка | YouTube → локальное видео через yt-dlp |
| 🎤 Транскрибация | OpenAI Whisper (CPU/GPU) |
| ✂️ Нарезка | Автоматически по сценам (PySceneDetect) |
| 🔇 Тишина | Удаление пауз через auto-editor |
| 📱 Вертикальный формат | Конвертация в 9:16 с blur-фоном |
| 📝 Субтитры | ASS/SSA стилизованные субтитры |
| 🏷️ Теги | AI-генерация хештегов |
| 🤖 LLM | Суммаризация через OpenAI или Ollama |
| 🎯 Моменты | AI-выделение лучших фрагментов |
| ☁️ Облако | Интеграция с Mail.ru Cloud |
| 🔒 Безопасность | Шифрование токенов пользователей |

---

## Быстрый старт

### Шаг 1 — Клонировать репозиторий

```bash
git clone https://github.com/Nestert/UploadBot.git
cd UploadBot
```

### Шаг 2 — Настроить переменные окружения

```bash
cp env_example.txt .env
```

Открой `.env` и заполни обязательные поля:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here   # обязательно — от @BotFather
OPENAI_API_KEY=sk-...                    # опционально — для LLM/Whisper API
LLM_PROVIDER=openai                      # openai | ollama
```

---

## 🐳 Запуск через Docker (рекомендуется)

Самый простой способ — работает на macOS, Linux и Windows 11.

```bash
# Сборка и запуск
docker-compose up -d

# Логи бота
docker-compose logs -f bot

# Остановка
docker-compose down
```

**Требования:** Docker Desktop (Windows/macOS) или Docker Engine (Linux).

---

## 🪟 Запуск на Windows 11

### 1. Системные требования

- Python 3.10+
- [FFmpeg](https://ffmpeg.org/download.html) — добавить в `PATH`
- Git

### 2. Установка FFmpeg на Windows

1. Скачай FFmpeg с https://ffmpeg.org/download.html → Windows builds by BtbN
2. Распакуй архив, например в `C:\ffmpeg`
3. Добавь `C:\ffmpeg\bin` в системный PATH:  
   *Пуск → Изменить переменные среды → Path → Создать → `C:\ffmpeg\bin`*
4. Проверь: `ffmpeg -version` в PowerShell

### 3. Установка зависимостей

Открой **PowerShell** или **Терминал** в папке проекта:

```powershell
# Создать виртуальное окружение
python -m venv venv

# Активировать
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

### 4. Запуск

```powershell
venv\Scripts\activate
python bot.py
```

---

## 🍎 Запуск на macOS / Linux

```bash
# Установить FFmpeg
brew install ffmpeg          # macOS
sudo apt install ffmpeg      # Ubuntu/Debian

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить
python bot.py
```

---

## Команды бота

| Команда | Описание |
|---|---|
| `/start` | Запуск бота |
| `/settings` | Настройки обработки |
| `/preview` | Превью видео |
| `/moments` | Лучшие моменты |
| `/llm` | LLM статус |
| `/gpu` | Проверка GPU |
| `/cache` | Статус кэша |
| `/mailru` | Подключить Mail.ru Cloud |

**Использование:** Отправь боту ссылку на YouTube — он сделает всё остальное.

---

## Структура проекта

```
UploadBot/
├── bot.py                    # Точка входа — Telegram бот
├── handlers/                 # Обработчики команд и событий
│   ├── commands.py           # Команды (/start, /settings, ...)
│   ├── callbacks.py          # Inline-кнопки
│   ├── message_handlers.py   # Обработка сообщений (ссылки)
│   ├── processing.py         # Основная обработка видео
│   ├── mailru_handlers.py    # Mail.ru Cloud
│   └── helpers.py            # Вспомогательные функции
├── download.py               # Скачивание YouTube видео
├── transcribe.py             # Транскрибация (Whisper)
├── scenes.py                 # Нарезка по сценам
├── autoedit.py               # Удаление тишины
├── vertical.py               # Вертикальный формат 9:16
├── subtitles.py              # ASS/SSA субтитры
├── tagging.py                # Генерация хештегов
├── llm.py                    # LLM-суммаризация
├── moments.py                # AI-лучшие моменты
├── mailru.py                 # Mail.ru Cloud API
├── cache.py                  # Кэширование
├── security.py               # Шифрование и безопасность
├── settings.py               # Настройки пользователей
├── utils.py                  # Утилиты
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── env_example.txt           # Шаблон .env
```

---

## Решение проблем

| Проблема | Решение |
|---|---|
| `TELEGRAM_BOT_TOKEN not set` | Проверь файл `.env` |
| `ffmpeg: command not found` | Установи FFmpeg и добавь в PATH |
| Whisper долго грузится | При первом запуске скачивает модель (~1.5 GB) |
| GPU не определяется | Проверь CUDA: `python -c "import torch; print(torch.cuda.is_available())"` |
| Ошибка `cryptography` | Обнови: `pip install cryptography==46.0.4` |

---

## Лицензия

MIT License

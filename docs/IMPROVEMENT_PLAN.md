# 📋 План улучшений UploadBot

> Документ содержит результаты анализа кодовой базы и рекомендации по улучшению проекта.

---

## 🏗️ 1. Архитектура и структура кода ✅

### 1.1 Разделение `bot.py` ✅ (Критично)

**Проблема:** Файл `bot.py` содержит 1256 строк — слишком много для одного модуля. Смешаны обработчики команд, бизнес-логика и UI-элементы.

**Решение:** ✅ Выполнено
```
handlers/
├── __init__.py
├── commands.py      # /start, /settings, /preview и т.д.
├── callbacks.py     # Обработчики callback-кнопок
├── processing.py    # Логика обработки видео
├── mailru_handlers.py # Обработчики Mail.ru  
├── message_handlers.py # YouTube-ссылки, видео
└── helpers.py       # CallbackUpdate, CallbackMessage
```

### 1.2 Класс `FakeUpdate` ✅ (Код-запах)

**Проблема:** В `handle_interface_callback()` создаются классы `FakeUpdate` и `FakeMessage` для имитации Telegram-объектов (строки 420-433).

**Решение:** ✅ Выполнено — вынесено в `handlers/helpers.py` как `CallbackUpdate`, `CallbackMessage` и `create_callback_update()`.

### 1.3 Дублирование очереди ✅

**Проблема:** `processing_queue` определена дважды (строки 34 и 49).

**Решение:** ✅ Выполнено — удалён дубликат при рефакторинге.

---

## 🔒 2. Безопасность ✅

### 2.1 Хранение токенов ✅

**Проблема:** Токены Mail.ru хранятся в JSON-файле без шифрования (`user_settings.json`).

**Решение:** ✅ Выполнено
- Создан модуль `security.py` с `encrypt_token` / `decrypt_token` (Fernet)
- Интегрировано в `settings.py`
- Добавлен `.gitignore`

### 2.2 Проверка входных данных ✅

**Проблема:** Нет валидации URL-адресов перед их обработкой.

**Решение:** ✅ Выполнено
- Добавлены `is_valid_youtube_url` и `is_valid_mailru_url` в `security.py`
- Интегрировано в `handlers/message_handlers.py` и `handlers/mailru_handlers.py`

---

## ⚡ 3. Производительность

### 3.1 Параллельная обработка сцен ✅

**Проблема:** Сцены обрабатываются последовательно в `run_processing_pipeline()`.

**Решение:** ✅ Выполнено — используется `asyncio.gather()` для параллельного запуска транскрибации и детекции сцен.

### 3.2 Кэширование моделей Whisper ✅

**Проблема:** Модель Whisper загружается при каждой транскрибации.

**Решение:** ✅ Выполнено — добавлено глобальное кэширование модели в `transcribe.py`.
```python
_whisper_cache = {}

def get_whisper_model(model_name, device):
    key = (model_name, device)
    if key not in _whisper_cache:
        _whisper_cache[key] = whisper.load_model(model_name, device=device)
    return _whisper_cache[key]
```

### 3.3 Очистка временных файлов

**Проблема:** WAV-файлы после транскрибации не удаляются автоматически.

**Решение:** Добавить cleanup после `run_whisper()` в `transcribe.py`.

---

## 🛠️ 4. Обработка ошибок

### 4.1 Неконсистентная обработка исключений

**Проблема:** В разных местах используются разные подходы к обработке ошибок. Некоторые функции возвращают `None`, другие выбрасывают исключения.

**Решение:** Ввести единый паттерн с кастомными исключениями:
```python
class UploadBotError(Exception):
    pass

class DownloadError(UploadBotError):
    pass

class TranscriptionError(UploadBotError):
    pass
```

### 4.2 Утечка ресурсов

**Проблема:** В нескольких местах файлы открываются через `open()` без `with` (строки 101, 965, 1031, 1087).

**Решение:** Заменить на контекстный менеджер:
```python
# Было:
await message.reply_video(open(fv, 'rb'), caption=...)

# Стало:
async with aiofiles.open(fv, 'rb') as f:
    await message.reply_video(f, caption=...)
```

---

## 📝 5. Логирование

### 5.1 Файл логов

**Проблема:** Логи пишутся только в stdout, файл `bot.log` (946KB) не подключён к logging.

**Решение:** Настроить FileHandler:
```python
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO,
    handlers=[
        logging.StreamHandler(),
        logging.FileHandler('bot.log', encoding='utf-8')
    ]
)
```

### 5.2 Ротация логов

**Проблема:** Файл логов растёт неограниченно.

**Решение:** Использовать `RotatingFileHandler`:
```python
from logging.handlers import RotatingFileHandler

handler = RotatingFileHandler(
    'bot.log', maxBytes=5*1024*1024, backupCount=3
)
```

---

## 🧪 6. Тестирование

### 6.1 Отсутствие тестов

**Проблема:** В проекте нет файлов с тестами.

**Решение:** Добавить структуру тестов:
```
tests/
├── __init__.py
├── test_download.py
├── test_transcribe.py
├── test_scenes.py
├── test_subtitles.py
└── conftest.py
```

### 6.2 Пример теста:
```python
# tests/test_scenes.py
import pytest
from scenes import get_video_duration, split_long_clip

def test_get_video_duration_invalid_file():
    result = get_video_duration("nonexistent.mp4")
    assert result is None

def test_split_long_clip_short_video(tmp_path):
    # Создать короткое тестовое видео
    # Убедиться что не разбивается
    pass
```

---

## 📦 7. Зависимости и Docker

### 7.1 Версионирование зависимостей

**Проблема:** `requirements.txt` не содержит версий:
```
python-telegram-bot
yt-dlp
openai-whisper
...
```

**Решение:** Закрепить версии для воспроизводимости:
```
python-telegram-bot==21.0
yt-dlp==2024.01.01
openai-whisper==20230918
```

### 7.2 `.dockerignore`

**Проблема:** Нет `.dockerignore`, в образ попадают лишние файлы.

**Решение:** Создать `.dockerignore`:
```
.git
__pycache__
*.pyc
*.log
venv/
cache/
videos/
.env
```

---

## 🌐 8. Локализация

### 8.1 Смешение языков в промптах LLM

**Проблема:** В `llm.py` используются китайские символы:
```python
"content": "Ты — помощник... Создай краткое,吸引人的 описание..."
```

**Решение:** Исправить на русский: "Создай краткое, привлекательное описание..."

---

## 📊 9. Мониторинг

### 9.1 Метрики

**Решение:** Добавить сбор метрик:
- Количество обработанных видео
- Время обработки по этапам
- Количество ошибок
- Использование ресурсов

---

## 🎯 Приоритеты

| Приоритет | Задача | Сложность | Влияние |
|-----------|--------|-----------|---------|
| 🔴 Высокий | Разделение `bot.py` | Высокая | Высокое |
| 🔴 Высокий | Исправление утечек ресурсов | Низкая | Высокое |
| 🔴 Высокий | Кэширование Whisper-модели | Низкая | Высокое |
| 🟡 Средний | Добавление тестов | Средняя | Среднее |
| 🟡 Средний | Настройка логирования | Низкая | Среднее |
| 🟡 Средний | Шифрование токенов | Средняя | Среднее |
| 🟢 Низкий | Локализация | Низкая | Низкое |
| 🟢 Низкий | `.dockerignore` | Низкая | Низкое |

---

## ✅ Следующие шаги

1. **Фаза 1:** Исправить критичные баги (утечки ресурсов, дублирование кода)
2. **Фаза 2:** Рефакторинг `bot.py` на модули
3. **Фаза 3:** Добавить базовое тестирование
4. **Фаза 4:** Улучшить производительность (кэширование)
5. **Фаза 5:** Настроить мониторинг и метрики
